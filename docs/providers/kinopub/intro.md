Введение — API 1.3 3 documentation

> 🔎 **Это вендорская документация, дословно.** Всё наше помечено 🔎 прямо у того метода, к
> которому относится: проверенное — со ссылкой на фикстуру и тесты, снятое с PWA и не
> проверенное — с явной пометкой. Считать непроверенное формой, а не контрактом.
>
> **Хост не один.** PWA ходит не на `api.service-kp.com`:
>
> | Хост | Префикс | Что это |
> | --- | --- | --- |
> | `api.boramoraboom.ru` | `/v1/…` | Тот же v1, что у нас |
> | `api.ios-kp.store` | `/api2/v1.1/…` | Другая ветка API — см. [video](video.md) и [collections](collections.md) |
>
> Картинки тоже разъезжаются: `m.staticpop.net`, `m.boramoraboom.ru`, `m.pushbr.com` (у подборок
> путь `/selection/…`). **Привязываться к домену нельзя нигде** — ни в кэше артворка, ни в фильтрах
> сетевого лога. Токен PWA возит как `access_token=` в query, как и описано ниже; мы шлём его
> заголовком — работают оба.
>
> Справочники (типы, жанры, страны, сортировки, разделы, качества) целиком лежат в одном публичном
> файле без авторизации — см. [references](references.md).

# Введение

Прежде чем осуществлять запросы к API требуется пройти аутентификацию. Получив access_token к каждому запросу API требуется добавлять GET параметр access_token=<полученный токен> Для всех запросов желательно включить cookie, в них передается csrf токен, без него POST запросы не будут проходить проверку.  

**Параметры запросов заключенные в::**

-

**[]** - необязательные
-

**<>** - обязательные
